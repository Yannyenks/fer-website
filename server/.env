# Development Environment Configuration
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8000

# Database (Local)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=jvepi
DB_USER=root
DB_PASS=

# Frontend origin allowed for CORS (multiple origins separated by comma)
FRONTEND_URL=http://localhost:3000,http://localhost:5173

# App secret for hashing IPs / tokens
APP_SECRET=change_me_to_a_random_string

# Admin credentials (initial, change after setup)
ADMIN_USER=admin
ADMIN_PASS=admin_password

# API URL for frontend
API_URL=http://localhost:8000
