<?php

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;
use App\Http\Controllers\Api\CandidateController;
use App\Http\Controllers\Api\ImageController;

Route::middleware('auth:sanctum')->group(function () {
    // Protected admin routes
    Route::apiResource('candidates', CandidateController::class);
    Route::apiResource('images', ImageController::class);
});

// Public read routes (optional)
Route::get('public/candidates', [CandidateController::class, 'index']);
Route::get('public/candidates/{id}', [CandidateController::class, 'show']);
Route::get('public/images', [ImageController::class, 'index']);

// Auth endpoints would be added (login/register via Sanctum)
